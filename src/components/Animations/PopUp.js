function popUp() {
    /* document.getElementById("filter").addEventListener("click", (e) => {
        e.preventDefault();
        let filterDisplayValue = document.getElementById("filter-More");
        let test = getComputedStyle(filterDisplayValue);
        let display = test.display;
        if (display == "none") {
          document.getElementById("filter-More").style.display = "block";
        } else {
          document.getElementById("filter-More").style.display = "none";
        }
      });
  
      document.getElementById("productButton").addEventListener("click", (e) => {
        e.preventDefault();
        document.getElementById("blur").classList.toggle("active");
        document.getElementById("popup").classList.toggle("active");
      });
  
      document.getElementById("popupCross").addEventListener("click", (e) => {
        e.preventDefault();
        document.getElementById("blur").classList.toggle("active");
        document.getElementById("popup").classList.toggle("active");
      }); */
}

export { popUp };

